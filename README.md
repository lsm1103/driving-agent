# driving-agent 文档索引

本项目研究一个问题：

> 一个没有人给它派活、但环境每天都在变的 Agent，连续运行 30 天，凭什么每天只挑其中几件事做？

不是再造一个 Agent Harness。Harness（Model / Tools / Skills / Memory / Loop / Sandbox）
是既有基础设施，直接复用。本项目只做它上面的一层：**动机与选择**。

## 文档

| 文档 | 内容 |
|---|---|
| [01-背景与问题定义](docs/01-背景与问题定义.md) | 从哪来、为什么现有方案不解决这个问题、问题的精确形式 |
| [02-目标与最终效果](docs/02-目标与最终效果.md) | 做完之后能看到什么、验收标准、明确不做什么 |
| [03-设计方案](docs/03-设计方案.md) | 原理（六条）、架构分层、数据结构、日循环、实现路径 |
| [04-验证与证伪](docs/04-验证与证伪.md) | A/B 对照实验、指标、写死的失败判据 |
| [05-模型与运行环境](docs/05-模型与运行环境.md) | DeepSeek 实测事实、结构化输出约束、成本、漂移探测 |
| [06-探针结果](docs/06-探针结果.md) | 真实世界实测：信号量、忽略率、五个改设计的发现 |

## 三个已定边界（已确认）

| 边界 | 选择 | 换掉的话 |
|---|---|---|
| 稀缺性 | 每日不结转的行动预算（主）+ 成本总盘（背景压力） | 改 03 文档 §2.1 与配置 `budget.*` |
| 环境 | 有心跳的世界：仓库 + 订阅源 + 收件箱，无指令 | 改 03 文档 §3.1 的 World Adapter 集合 |
| 30 天产出 | 被事实检验过的世界模型/自我模型（预测—对账） | 改 02 文档 §3 与 04 文档的主指标 |

这三条是整个设计的地基。任何一条改动都会往下传导，改之前先读 01 文档第 3 节。

## 技术栈

模型 `deepseek-v4-pro`（OpenAI 兼容协议直连），凭证在 `.env`（已 gitignore）。
模型侧的每一条事实都经过实测验证，不是从文档推断——见
[05-模型与运行环境](docs/05-模型与运行环境.md)。

三处与原设计冲突、已导致设计改动的实测结论：

- 严格 JSON Schema **不可用** → 审议层改走函数调用 + 代码校验 + 有界重试
- 强制 `tool_choice` 在 thinking 模式下**不可用** → 只能 `auto` + 强提示
- 输入缓存命中比未命中便宜 **30 倍** → 「账本前缀逐字节稳定」升级为一等设计约束

## 当前状态

**探针已完成两轮**（[probe/](probe/README.md)，结果见 [06](docs/06-探针结果.md)）。
在真实世界上实测 1068 条信号 / 7 天，三项修正全部落地并回放验证：

| | v1 | v2 |
|---|---|---|
| 判断覆盖率 / 幽灵编号 | 99.53% / 4 | **100% / 0** |
| 日均登记 vs 5 槽预算 | 12.25（净流入 +7.25/天） | **4.8（净流入 −0.2/天）** |
| exploration 占比 | 69% | 71%（**未解决**） |

World 选型无需推翻，判据可用，可以进入阶段 0。

**但阶段 0 开工前有三件事必须先做**（全部来自探针实测）：

1. **主判据改为多次重复 + 方差估计。** 同一输入跑两次，逐条不一致率仅 2.7%，
   却因登记基率低而放大成 **+75%** 的计数波动——单次 30 天运行不足以区分 A/B 组
2. **给 Agent 建独立沙盒仓库并接上 `RepoAdapter`。** 当前 World 只有"进来的"、
   没有"我的"，4 个 kind 里 maintenance 和 social 全无信号源，驱动冲突不会发生
3. **在 Compete 阶段实现探索配额。** 收紧 Intake 判据只减少总量、不改变构成，
   探索偏斜是一条独立约束